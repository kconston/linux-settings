local function init()
--tray = {
  --  height = 10,
  --  position = "bottom" -- Can be "bottom" or "top"
  --},
  --floating = {
	--	--elements = {
	--	--	"repl"
	--	--},
  --  max_height = .5, -- These can be integers or a float between 0 and 1.
  --  max_width = 20   -- Floats will be treated as percentage of your screen.
  --}

	--require("dapui").setup({
  --icons = {
  --  expanded = "⯆",
  --  collapsed = "⯈",
  --  circular = "↺"
  --},
  --mappings = {
  --  expand = "<CR>",
  --  open = "o",
  --  remove = "d"
  --},
  --sidebar = {
  --  elements = {
  --    -- You can change the order of elements in the sidebar
  --    "scopes",
  --    "stacks",
  --    --"watches"
  --  },
  --  width = 40,
  --  position = "left" -- Can be "left" or "right"
  --},
  --})
end

return {
	init = init
}
